/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      remotePatterns: [
        {
            protocol: 'https',
            hostname: 'server-h9f2c9slm-enriquesuarezzzs-projects.vercel.app',
          },
      ],
    },
}

export default nextConfig;
